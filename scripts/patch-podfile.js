const fs = require("fs");
const path = require("path");

// Disabled - RNWorklets removed from project
console.log("ℹ️ RNWorklets patch script disabled - RNWorklets removed from dependencies.");
process.exit(0);
